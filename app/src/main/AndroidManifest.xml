<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="actualsolusi.com.samplekelasb">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="Main Page" />
        <activity
            android:name=".CalculatorActivity"
            android:label="Calculator" />
        <activity android:name=".ParsingNilaiActivity" />
        <activity android:name=".ContohPreferenceActivity" />
        <activity android:name=".MyPreferencesActivity" />
        <activity
            android:name=".SampleRecyclerView"
            android:label="List Of Country" />
        <activity android:name=".DetailCountryActivity" />
        <activity
            android:name=".TambahCountryActivity"
            android:label="Tambah Country" />
        <activity
            android:name=".DokterActivity"
            android:label="List Of Dokter">

        </activity>
        <activity
            android:name=".TambahDokterActivity"
            android:label="Tambah Data Dokter" />
        <activity
            android:name=".EditDokterActivity"
            android:label="Edit Dokter" />
        <activity android:name=".ProgressDialogActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>